#include <stdio.h>
/**
 * main (-Entry point) C Program that prints a line with puts function not printf
 * Return: 0(Success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
